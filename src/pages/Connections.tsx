import { FC } from 'react'

interface ConnectionsProps {
    props?: string
}

const Connections: FC<ConnectionsProps> = ({ props }) => {
    return <div>Connections</div>
}

export default Connections